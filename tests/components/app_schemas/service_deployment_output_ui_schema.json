{
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "enum": ["HTTP API"]
    },
    "__type__": {
      "type": "string",
      "enum": ["WebApp"]
    },
    "Api Type": {
      "type": "string",
      "enum": ["webapp"]
    },
    "Hostname": {
      "type": "string",
      "minLength": 5
    },
    "Protocol": {
      "type": "string",
      "enum": ["http", "https"]
    },
    "Port": {
      "type": "string",
      "pattern": "^[0-9]+$"
    },
    "Connection Timeout": {
      "type": "string",
      "pattern": "^[0-9]+$"
    },
    "Base Path": {
      "type": "string",
      "enum": ["/"]
    }
  },
  "required": [
    "title",
    "__type__",
    "Api Type",
    "Hostname",
    "Protocol",
    "Port",
    "Connection Timeout",
    "Base Path"
  ],
  "additionalProperties": false
}
