name: Nightly Tests Run Workflow

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:
permissions:
  contents: write
  pages: write

jobs:
  test:
    name: Run Tests
    permissions:
      contents: write
      pull-requests: write
    uses: ./.github/workflows/run_test.yaml
    secrets: inherit
