name: Nightly Tests Run Workflow

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/run_test.yaml
    with:
      destination_dir: run-test-latest
    secrets: inherit
